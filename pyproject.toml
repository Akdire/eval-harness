[project]
name = "eval-harness"
version = "0.1.0"
description = "A small, focused eval harness for evaluating AI systems in Python."
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]